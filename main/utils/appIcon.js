import { Platform } from 'react-native';
import { changeIcon } from 'react-native-change-icon';

export const setAppIcon = async _theme => {
    // Keep dynamic theme in-app without killing the running task.
    return;
};

export default {
    setAppIcon,
};
